{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "NxMelos",
  "title": "Generates the configuration to use Melos to manage Flutter/Dart projects in the workspace",
  "type": "object",
  "properties": {
    "scriptNameSeparator":{
      "description": "Separator to use to name melos scripts added in the root package.json file (for e.g 'melos-bootstrap' or 'melos:bootstrap')",
      "default": "-",
      "type": "string",
      "enum": [":", "-"],
      "x-prompt": {
        "message": "What separator would you like to use to generate the name of melos scripts added in the root package.json file? (for e.g 'melos-bootstrap' or 'melos:bootstrap')"
      }
    }
  }
}
